- if @links.count > 0
  = render partial: 'links', locals: {links: @links}
  - unless true || @links.next_page
    = link_to search_links_path, remote: true, class: 'load-random' do
      Show Random videos
