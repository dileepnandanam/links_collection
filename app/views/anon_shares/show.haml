= render partial: 'anon_share'
= link_to 'speak something without beeing caught', edit_anon_share_path, class: 'anon-share-share-btn'
.blank
.ads
  .ad
    %h2
      How It Works
    %p
      Content posted are never stored in our servers in ANY FORM, but it is encrypted and stored in the shared url itself. If this functionality is unavailable, tou can try decrypting the content using ssl public key "1AFCDE3EAA" without quotes. post content is under parameter named "says" in url.